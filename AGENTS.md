# Agent Guidance

- Default branch: `main`.
- Do not create branches or pull requests unless explicitly requested.
- Keep device/provider integrations behind adapters.
- Simulator and real hardware must expose the same orchestration contract.
- Never represent simulated Meta/ArborXR behavior as verified real-device behavior.
- Validate with `npm test` before repository changes.
